import 'package:flutter/material.dart';

/// Care Schedule — Color Palette
/// Extracted from the "Care Schedule" mockup.
class Colors {
  Colors._();

  /// Background
  static const Color background = Color(0xFFFAF8F4);

  /// Primary text (headings)
  static const Color textPrimary = Color(0xFF2E2C28);

  /// Muted / label text ("THIS WEEK", day abbreviations)
  static const Color textMuted = Color(0xFF8C8779);

  /// Green — accent (selected day, active card, NOW badge)
  static const Color accentGreen = Color(0xFF5B8A72);

  /// Green tint (active card background)
  static const Color accentGreenTint = Color(0xFFE5EFE8);

  /// Blue — Sarah
  static const Color sarah = Color(0xFF6C8FC7);

  /// Orange — Mike
  static const Color mike = Color(0xFFE0A458);

  /// Pink / Rose — Lisa
  static const Color lisa = Color(0xFFD98894);

  /// Purple — James (Nurse)
  static const Color james = Color(0xFF9B87B5);

  /// Card white
  static const Color card = Color(0xFFFFFFFF);
}