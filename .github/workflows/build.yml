name: Flutter Build

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Prepare env file for build assets
        working-directory: refactored_pancake
        run: cp .env.example .env

      - name: Get dependencies
        working-directory: refactored_pancake
        run: flutter pub get

      - name: Build APK
        working-directory: refactored_pancake
        run: flutter build apk --release
